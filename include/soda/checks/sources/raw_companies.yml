checks for raw_companies:
  - schema:
      fail:
        when required column missing: [shortName,city,state,zip,country,industry,enterpriseValue,simbolo]
        when wrong column type:
          city: string
          shortName: string
          state: string
          zip: string
          country: string
          industry: string
          enterpriseValue: int64
          simbolo: string